<?php
require __DIR__ . '/../../config/bootstrap.php';

if (YII_ENV === 'dev') {
    hiqdev\composer\config\Builder::rebuild();
}

$config = require \hiqdev\composer\config\Builder::path('admin');

(new \codexten\yii\web\Application($config))->run();